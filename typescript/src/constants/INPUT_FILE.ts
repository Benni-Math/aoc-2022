import INPUT_DIR from './INPUT_DIR';

/**
 * Enum-style object to keep track of input files.
 * @author Benedikt Arnarsson
 */
const INPUT_FILE = {
  DAY_1: `${INPUT_DIR}/01.txt`,
} as const;

export default INPUT_FILE;
